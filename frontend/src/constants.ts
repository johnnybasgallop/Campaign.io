// Mirrors the batch/delay constants in the backend messaging.py
// Used by ProgressCard to estimate time remaining
export const BATCH_SIZE = 50;
export const BATCH_PAUSE = 60;
export const AVG_MSG_DELAY = 5.5;
